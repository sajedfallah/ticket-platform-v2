export default function Navbar() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 flex justify-around bg-black/80 p-4 text-white">
      <button>Home</button>
      <button>Events</button>
      <button>Tickets</button>
      <button>Profile</button>
    </nav>
  );
}
